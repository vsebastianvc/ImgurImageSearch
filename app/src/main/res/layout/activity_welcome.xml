<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".views.activities.WelcomeActivity" >


    <ImageView
        android:id="@+id/splashApplication"
        android:layout_width="wrap_content"
        android:layout_height="379dp"
        android:layout_gravity="center_horizontal"
        android:layout_weight="6"
        android:adjustViewBounds="true"
        android:contentDescription="@string/description_logo_aplicacion"
        android:maxWidth="@dimen/logo_aplication_max_heigh"
        android:maxHeight="@dimen/logo_aplication_max_width"
        android:scaleType="fitCenter"
        android:src="@drawable/welcome" />

</LinearLayout>